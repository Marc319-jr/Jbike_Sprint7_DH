<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="/image/Logos/Logophone.png" />
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@100&display=swap" rel="stylesheet">
    <link rel="stylesheet" href=" https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.11.2/css/all.min.css ">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
    <link rel="stylesheet" href="../../css/estilosLogin.css">
    <title>JBike</title>

</head>

<body>
    <div class="container-login">
        <div class="background-login">
            <img src = "../image/login/registerimage3.jpeg" width="100%" height = "100%" >
        </div>
        <div class="form-login">
            <div class="form-container">

            
                <a href="/">
                    <img src="../image/Logos/Logophone.png" width = 130px>
                </a>
                <div class="ingreso">
    
                    <h3>Crear una cuenta</h3>
                    <form action="/users/register" method="POST" enctype="multipart/form-data" class = "formRegister">
                        <% if (locals.errores && errores.nombreUsuario) { %>
                            <p class="errorBack"> <%= errores.nombreUsuario.msg  %> ! </p>
                        <% } %>
                        <div class = "inputs">
                        <p id = "nombreError" class = "errorFront"></p>
                        <input type="text" name="nombreUsuario" placeholder="Nombre y Apellido" value = "<%= locals.oldData ? oldData.nombreUsuario : null  %>" id="nombreUsuario" class ="input" >
                        <% if (locals.errores && errores.imagen) { %>
                            <p class="errorBack"> <%= errores.imagen.msg  %> ! </p>
                        <% } %>
                        <p id = "emailError" class = "errorFront"></p>
                        <input type="email" name="emailUsuario" placeholder="Correo electronico" class="input" id = "email"  >
                        <span id = "errorEmail"></span>
                        <% if (locals.errores && errores.emailUsuario) { %>
                            <p class="errorBack"> <%= errores.emailUsuario.msg  %> ! </p>
                        <% } %>
                        <p id = "passError" class = "errorFront"></p>
                        <input type="password" name="passwordUsuario" placeholder="Contraseña" class="input" id = "password"> 
                        <span id = "errorPass"></span>
                        <% if (locals.errores && errores.passwordUsuario) { %>
                            <p class="errorBack"> <%= errores.passwordUsuario.msg  %> ! </p>
                        <% } %>    
                        <p id = "confrimpassError" class = "errorFront"></p>
                        <input type="password" name = "confirmarPass" placeholder="Confirme la contaseña" class = "input" id ="passConfirm">
                        <p id = "phoneError" class = "errorFront"></p>
                        <input type="text" name = "telefono" placeholder="Numero de telefono" id = "telefono">
                        <select name="genero" class="input" style = "margin-top: 10px; margin-bottom: -10px;">
                            <option value ="" name  = "vacio">Seleccione el genero</option>
                            <% let opciones = ['Hombre' , 'Mujer'] %>
                            <% for(let i = 0;i<opciones.length;i++ ) { %>
                                <option value = "<%= opciones[i] %>" <%= locals.oldData && (opciones[i] === oldData.genero) ? 'selected' : null %> name = "genero"> <%= opciones[i] %>  </option>
                            <% } %>
                        
                        </select>
                    </div>
  
                    <% if (locals.errores && errores.genero) { %>
                        <p class="errorBack"> <%= errores.genero.msg  %> ! </p>
                    <% } %>
                    <br>
                    <p id = "imageError" class = "errorFront"></p>
                    <label for = "imagen">Ingrese su imagen de perfil</label>
                    <input type="file" name = "imagen">
                    <div class="buttons">
                        <a href="/index"></a>   
                            <button type = "submit" class="login-botton">
                                Ingresar
                            </button>
                     
                    </div>
                    Ya tenes una cuenta? <a href="/users/login">Ingresa aca</a>
                </form>
                </div>
        </div>
    </div>
    </div>

</body>
<script src = "/js/validacion-register.js"></script>


</html>